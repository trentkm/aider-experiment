# To run the Rust file, execute the following command in your terminal:
# cargo run

# This is a Python file, not a Rust file.
# The command above is for running a Rust file, not this Python file.

def main():
    print("This is a Python script, not a Rust script.")

if __name__ == "__main__":
    main()
